---
phase: 06-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/book.toml
  - docs/src/SUMMARY.md
  - docs/src/README.md
  - docs/src/scripting-reference/scene.md
  - docs/src/scripting-reference/dialogue.md
  - docs/src/scripting-reference/audio.md
  - docs/src/scripting-reference/flow.md
  - docs/src/scripting-reference/menu.md
  - docs/src/scripting-reference/variables.md
  - docs/src/scripting-reference/conditionals.md
  - docs/src/scripting-reference/save-load.md
  - docs/src/scripting-reference/ui-theming.md
  - docs/src/scripting-reference/text-markup.md
  - docs/theme/favicon.svg
autonomous: true
requirements:
  - D-03: Scripting Reference
  - D-03: Getting Started (scaffolding)
user_setup: []
must_haves:
  truths:
    - "Developer can install mdBook and build the docs site locally"
    - "Developer can navigate the documentation via sidebar table of contents"
    - "Developer can find documentation for every .crka op organized by functional category"
    - "Developer can see a landing page introducing Cereka as a game engine"
  artifacts:
    - path: "docs/book.toml"
      provides: "mdBook configuration (title, theme, search, output)"
      min_lines: 25
    - path: "docs/src/SUMMARY.md"
      provides: "Table of contents / sidebar navigation"
      min_lines: 50
    - path: "docs/src/README.md"
      provides: "Landing page / introduction to Cereka"
      min_lines: 30
    - path: "docs/src/scripting-reference/*.md"
      provides: "Complete scripting language documentation across 10 sub-pages"
      exports: ["scene.md", "dialogue.md", "audio.md", "flow.md", "menu.md", "variables.md", "conditionals.md", "save-load.md", "ui-theming.md", "text-markup.md"]
    - path: "docs/theme/favicon.svg"
      provides: "Browser tab icon"
      min_lines: 3
  key_links:
    - from: "docs/book.toml"
      to: "docs/src/SUMMARY.md"
      via: "mdBook src directory configuration"
      pattern: "src"
    - from: "docs/src/SUMMARY.md"
      to: "docs/src/scripting-reference/*.md"
      via: "mdBook chapter links"
      pattern: "scripting-reference"
---

<objective>
Create the mdBook documentation site scaffolding and write the complete Scripting Reference section covering all 38 .crka ops.

**Purpose:** Give game authors a browsable, searchable reference for every .crka scripting command, organized by functional category (scene, dialogue, audio, flow, menus, variables, conditionals, save/load, UI theming, text markup).

**Output:** 
- `docs/book.toml` — mdBook configuration
- `docs/src/SUMMARY.md` — full table of contents (including links to pages written in Plan 06-02)
- `docs/src/README.md` — landing page
- `docs/src/scripting-reference/*.md` — 10 sub-pages covering all 38 ops
- `docs/theme/favicon.svg` — browser tab icon
</objective>

<execution_context>
@/Users/danialhaikal/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/danialhaikal/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-the-complete-documentation-and-a-proper-documentation-site-l/06-CONTEXT.md
@.planning/phases/06-the-complete-documentation-and-a-proper-documentation-site-l/06-RESEARCH.md

## Canonical Source Files (must read before writing)

@src/compiler/cereka_instruction.hpp
@src/cereka_ui_config.hpp
@src/cereka_save_data.hpp
@launcher/templates.hpp
@CLAUDE.md (lines 64-142 — existing .crka language summary)

## Verified Compile Test Inputs (canonical example snippets)

@tests/compile/inputs/basic.crka
@tests/compile/inputs/variables.crka
@tests/compile/inputs/ui_theme.crka
@tests/compile/inputs/save_load.crka
@tests/compile/inputs/text_markup.crka
@tests/compile/inputs/flow.crka
@tests/compile/inputs/menu.crka
@tests/compile/inputs/audio.crka

## Example of op documentation format (from RESEARCH.md)

Each op in the scripting reference must follow this pattern:
- Syntax block (```text)
- Parameter table (| Parameter | Required | Type | Description |)
- Behavior description
- Example(s) in ```text
- Notes / edge cases

See RESEARCH.md lines 178-210 for full example (`bg` op documentation template).

## Theme

Research recommends default mdBook light theme with `preferred-dark-theme = "navy"`. Use `additional-css = ["theme/custom.css"]` for minimal branding. Favicon: simple "C" letter or SVG text icon — per agent's discretion (CONTEXT.md D-agent's Discretion).
</context>

<tasks>

<task type="auto">
  <read_first>
    - .planning/phases/06-the-complete-documentation-and-a-proper-documentation-site-l/06-RESEARCH.md (lines 264-275 — mdBook install and build commands)
    - .planning/phases/06-the-complete-documentation-and-a-proper-documentation-site-l/06-RESEARCH.md (lines 289-318 — book.toml example, SUMMARY.md structure)
  </read_first>
  <name>Task 1: Scaffold docs directory — install mdBook, create book.toml, SUMMARY.md, README.md, favicon</name>
  <files>
    - docs/book.toml
    - docs/src/SUMMARY.md
    - docs/src/README.md
    - docs/theme/favicon.svg
  </files>
  <action>
    1. Install mdBook via Homebrew: `brew install mdbook`. Verify with `mdbook --version` — must report 0.5.x.
    2. Create the docs/ directory structure:
       ```
       docs/
       ├── book.toml
       ├── src/
       │   ├── SUMMARY.md
       │   ├── README.md
       │   ├── getting-started.md        (placeholder — written in Plan 06-02)
       │   ├── project-structure.md      (placeholder — written in Plan 06-02)
       │   ├── build-and-package.md      (placeholder — written in Plan 06-02)
       │   ├── scripting-reference/
       │   │   ├── scene.md
       │   │   ├── dialogue.md
       │   │   ├── audio.md
       │   │   ├── flow.md
       │   │   ├── menu.md
       │   │   ├── variables.md
       │   │   ├── conditionals.md
       │   │   ├── save-load.md
       │   │   ├── ui-theming.md
       │   │   └── text-markup.md
       │   ├── ui-theming.md            (placeholder — written in Plan 06-02)
       │   ├── variables-and-expressions.md (placeholder — written in Plan 06-02)
       │   └── examples/
       │       └── annotated-game.md    (placeholder — written in Plan 06-02)
       └── theme/
           └── favicon.svg
    3. Write `docs/book.toml` with these exact values (per RESEARCH.md lines 289-318 and CONTEXT.md D-01):
       ```toml
       [book]
       title = "Cereka Game Engine Documentation"
       authors = ["Cereka Contributors"]
       description = "Complete guide to writing games with Cereka and the .crka scripting language."
       language = "en"

       [build]
       build-dir = "book"
       create-missing = false

       [output.html]
       default-theme = "light"
       preferred-dark-theme = "navy"
       git-repository-url = "https://github.com/user/cereka"
       edit-url-template = "https://github.com/user/cereka/edit/main/docs/{path}"
       site-url = "/"
       no-section-label = false
       additional-css = ["theme/custom.css"]

       [output.html.search]
       enable = true
       limit-results = 20

       [output.html.print]
       enable = true
       page-break = true
       ```
       Use placeholder `https://github.com/user/cereka` for git URLs — the user will replace with their actual repo URL.

    4. Write `docs/src/SUMMARY.md` — the complete table of contents. It MUST include links to ALL pages including those written in Plan 06-02 (getting-started.md, project-structure.md, build-and-package.md, ui-theming.md, variables-and-expressions.md, examples/annotated-game.md). Structure follows RESEARCH.md lines 324-356:
       ```markdown
       # Summary

       [Introduction](README.md)

       # User Guide

       - [Getting Started](getting-started.md)
       - [Project Structure](project-structure.md)
       - [Build & Package](build-and-package.md)

       # Scripting Reference

       - [Scene Commands](scripting-reference/scene.md)
       - [Dialogue Commands](scripting-reference/dialogue.md)
       - [Audio Commands](scripting-reference/audio.md)
       - [Flow Control](scripting-reference/flow.md)
       - [Menus & Choices](scripting-reference/menu.md)
       - [Variables](scripting-reference/variables.md)
       - [Conditionals](scripting-reference/conditionals.md)
       - [Save & Load](scripting-reference/save-load.md)
       - [UI Theming](scripting-reference/ui-theming.md)
       - [Text Markup](scripting-reference/text-markup.md)

       # Theming & Configuration

       - [UI Theming Reference](ui-theming.md)
       - [Variables & Expressions](variables-and-expressions.md)

       # Examples

       - [Annotated Example Game](examples/annotated-game.md)
       ```

    5. Write `docs/src/README.md` — landing page introducing Cereka. Content:
       - Brief description: "Cereka is a visual novel game engine where authors write only .crka scripts — no C++ needed."
       - Key features list (from CLAUDE.md "Implemented features" section — lines 204-205, but in game-author-friendly language)
       - Navigation guidance: what each section covers
       - A "Quick Start" link: "Jump to Getting Started →"
       - Tone: welcoming, practical, no buzzwords. Target: game author who knows Ren'Py or similar.

    6. Create `docs/theme/favicon.svg` — simple SVG favicon. A "C" letter mark in a hex shape or minimal circle, using a game-console-inspired color (#4CAF50 green or #2196F3 blue). Keep SVG under 20 lines — no external fonts, use system fonts (Arial/sans-serif). This is per CONTEXT.md agent's discretion.

    IMPORTANT: Create placeholder files for pages that will be written in Plan 06-02 (getting-started.md, project-structure.md, build-and-package.md, ui-theming.md, variables-and-expressions.md, examples/annotated-game.md). Each placeholder should be a single line: `<!-- This page is written in Plan 06-02. -->`. This ensures mdBook can build without errors (create-missing is false).
  </action>
  <verify>
    <automated>ls docs/book.toml docs/src/SUMMARY.md docs/src/README.md docs/theme/favicon.svg && mdbook --version && mdbook build docs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mdbook build docs` succeeds (produces docs/book/ directory)
    - `docs/book/index.html` exists and is valid HTML
    - `docs/book/searchindex.json` exists (search enabled)
    - SUMMARY.md has links to all 18+ pages
    - Placeholder files exist for pages written in Plan 06-02
    - favicon.svg renders as a recognizable "C" icon
  </acceptance_criteria>
</task>

<task type="auto">
  <read_first>
    - src/compiler/cereka_instruction.hpp (full Op enum — all 38 ops)
    - tests/compile/inputs/basic.crka (scene commands)
    - tests/compile/inputs/flow.crka (flow control examples)
    - tests/compile/inputs/menu.crka (menu examples)
    - tests/compile/inputs/audio.crka (audio examples)
    - tests/compile/inputs/save_load.crka (save/load examples)
    - tests/compile/inputs/text_markup.crka (text markup examples)
    - src/cereka_ui_config.hpp (UI config properties — for scripting-reference/ui-theming.md)
    - .planning/phases/06-the-complete-documentation-and-a-proper-documentation-site-l/06-RESEARCH.md (lines 178-210 — op documentation format pattern)
  </read_first>
  <name>Task 2: Write Scripting Reference — 10 sub-pages covering all 38 ops</name>
  <files>
    - docs/src/scripting-reference/scene.md
    - docs/src/scripting-reference/dialogue.md
    - docs/src/scripting-reference/audio.md
    - docs/src/scripting-reference/flow.md
    - docs/src/scripting-reference/menu.md
    - docs/src/scripting-reference/variables.md
    - docs/src/scripting-reference/conditionals.md
    - docs/src/scripting-reference/save-load.md
    - docs/src/scripting-reference/ui-theming.md
    - docs/src/scripting-reference/text-markup.md
  </files>
  <action>
    Write 10 markdown files, each covering a category of .crka ops. Every op MUST follow this exact format (per RESEARCH.md lines 178-210):

    ```markdown
    ## `<op>` — Short Description

    One-sentence explanation.

    **Syntax:**
    ```text
    <op> <param1> [optional_param] <param2>
    ```

    **Parameters:**
    | Parameter | Required | Type | Description |
    |-----------|----------|------|-------------|
    | `param1` | yes | string | What this does |

    **Behavior:**
    - Bullet points explaining what happens at runtime

    **Examples:**
    ```text
    ; Example 1
    <op> forest.png
    ```

    **Notes:**
    - Edge cases, limitations, caveats
    ```

    Group ops into these 10 files referencing the Op enum (src/compiler/cereka_instruction.hpp) and CLAUDE.md .crka language section (lines 64-142):

    1. **scene.md** — BG, CHAR, HIDE_CHAR, FADE (4 ops)
       - Document bg <file>, bg <file> fade <sec>, char syntax and positions, hide char
       - Source: cereka_instruction.hpp Op enum, tests/compile/inputs/basic.crka

    2. **dialogue.md** — SAY, NARRATE (2 ops)
       - Document say <id> "text", narrate "text", {var} substitution behavior
       - Explain difference between say (shows name box) and narrate (no name box)
       - Source: cereka_instruction.hpp, CLAUDE.md lines 76-78

    3. **audio.md** — PLAY_BGM, STOP_BGM, PLAY_SFX, PLAY_BGM_FADE, STOP_BGM_FADE, BGM_CROSSFADE (6 ops)
       - bgm <file>, stop_bgm, sfx <file>, bgm fade <file> <sec>, stop_bgm fade <sec>, bgm crossfade <file1> <file2> <sec>
       - Source: cereka_instruction.hpp + src/state/cereka_states.cpp case handlers (lines 199, 215, 226)
       - Note: supported audio formats (wav, ogg from STACK.md)

    4. **flow.md** — LABEL, JUMP, INCLUDE, CALL, RETURN, END (6 ops)
       - label, jump, include (compile-time inline), call (runtime subroutine), end
       - CALL stack limit of 32 (from CLAUDE.md line 88)
       - Source: cereka_instruction.hpp, tests/compile/inputs/flow.crka

    5. **menu.md** — MENU, BUTTON (2 ops)
       - menu block, button "Text" goto <label>, button "Text" exit
       - Optional bg inside menu (with optional fade)
       - Source: cereka_instruction.hpp, tests/compile/inputs/menu.crka, CLAUDE.md lines 93-96

    6. **variables.md** — SET_VAR, SET_VAR_NUM (2 ops via set and $ syntax)
       - set <var> <value> (string)
       - $ <var> = <expr>, $ <var> +=/-=/*=/= <expr> (numeric arithmetic)
       - Expression RHS with * / precedence over + -
       - Source: cereka_instruction.hpp, tests/compile/inputs/variables.crka, CLAUDE.md lines 98-109

    7. **conditionals.md** — IF_EQ, IF_NEQ, IF_GT, IF_LT, IF_GE, IF_LE, ELSE, ENDIF (8 ops)
       - All six comparison operators: ==, !=, >, <, >=, <=
       - else block, endif
       - String comparison (==, !=) and numeric comparison (all six)
       - Source: cereka_instruction.hpp, tests/compile/inputs/variables.crka, CLAUDE.md lines 111-119

    8. **save-load.md** — SAVE, LOAD, SAVE_MENU, LOAD_MENU (4 ops)
       - save <1-10>, load <1-10> (silent)
       - save_menu (10-slot overlay), load_menu (10-slot overlay with ESC cancel)
       - File format: saves/slot{N}.json
       - Source: cereka_instruction.hpp, tests/compile/inputs/save_load.crka, cereka_save_data.hpp

    9. **ui-theming.md** — UI_SET (1 op with multiple elements)
       - ui textbox (color, y, h, text_margin_x, text_color, image, wrapWidth, lineSpacing)
       - ui namebox (color, x, y_offset, w, h, text_color, image)
       - ui button (color, w, h, text_color, image, hover_image)
       - ui font (size)
       - ui advance_keys (space, enter, mouse1, etc.)
       - Note: Dim type supports % and pixels for y, h, wrapWidth
       - Source: cereka_ui_config.hpp (every property), tests/compile/inputs/ui_theme.crka, launcher/templates.hpp

    10. **text-markup.md** — Render-time inline text markup (not a separate Op)
        - <b>bold</b>, <i>italic</i>, <color=#rrggbb>colored text</color>
        - Used inside say and narrate strings
        - Source: tests/compile/inputs/text_markup.crka

    IMPORTANT STYLE RULES (per RESEARCH.md Anti-Patterns):
    - Use ```text for all .crka code blocks (highlight.js has no .crka definition — RESEARCH.md Pitfall 1)
    - NO C++ internals (no mention of CerekaImpl, sol2, glaze, Op enum, Lua, state machine — per D-02)
    - Every example should be verifiable — use compile test inputs as canonical sources
    - Include source-location diagnostics note where relevant (instruction carries line/col for error reporting)
    - Separate op pages by function, not alphabetically (RESEARCH.md Pattern 1)
  </action>
  <verify>
    <automated>ls docs/src/scripting-reference/*.md | wc -l && mdbook build docs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 10 markdown files exist in docs/src/scripting-reference/
    - Every Op from cereka_instruction.hpp (38 ops) appears in at least one file
    - Each op has syntax, parameter table, behavior description, and at least one example
    - Code blocks use ```text (not ```crka)
    - No C++ internal terms (CerekaImpl, sol2, glaze, state machine, renderer, Lua)
    - `mdbook build docs` succeeds with zero warnings
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries exist — this is a documentation-only phase with no executable code, no authentication, no data storage, and no network endpoints. The mdBook static site has no server-side processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | Cloudflare Pages custom domain DNS | accept | CAA records may need updating (RESEARCH.md Pitfall 4); user must ensure Cloudflare CAs are allowed |
| T-06-02 | T | mdBook build command in CI | accept | mdBook binary download via curl (no credentials needed); command injection risk is zero for static markdown content |
</threat_model>

<verification>
- `mdbook build docs` succeeds with zero errors and zero warnings
- All 10 scripting-reference/*.md files exist and are non-empty
- Each op documented has syntax block, parameter table, behavior, and at least one example
- No C++ internals terminology in any prose
- `docs/book/index.html` renders in browser with sidebar navigation and search bar
- Favicon displays in browser tab
</verification>

<success_criteria>
- mdBook 0.5.x installed
- `docs/` directory has complete structure with book.toml, SUMMARY.md, README.md, 10 scripting reference pages, favicon, and placeholder pages for Plan 06-02
- `mdbook build docs` completes successfully
- All 38 ops documented across 10 category pages
- Place for Plan 06-02 content reserved in SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-the-complete-documentation-and-a-proper-documentation-site-l/06-01-SUMMARY.md`
</output>
