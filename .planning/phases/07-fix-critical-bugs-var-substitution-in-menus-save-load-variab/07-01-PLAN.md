# Plan 07-01: Named Checkpoint System

## Goal
Add `checkpoint store "name"` / `checkpoint load "name"` as a dedicated in-memory variable snapshot system. Clean up the `SaveCheckpoint`/`LoadCheckpoint` stubs that are no longer needed.

## Files

| # | File | Change |
|---|------|--------|
| 1 | `src/cereka_script_interpreter.hpp` | Add `CheckpointData` struct + `std::unordered_map<std::string, CheckpointData> checkpoints` to `ScriptInterpreter` |
| 2 | `scripts/cereka_compiler.lua` | Parse `checkpoint store "<name>"` / `checkpoint load "<name>"` → AST → lower to ops |
| 3 | `src/compiler/cereka_instruction.hpp` | Add `CHECKPOINT_STORE`, `CHECKPOINT_LOAD` to `Op` enum |
| 4 | `src/compiler/cereka_instruction.cpp` | Map `"checkpoint_store"` / `"checkpoint_load"` string ops → Op |
| 5 | `src/state/cereka_states.cpp` | Dispatch: STORE saves vars to named entry; LOAD copies back |
| 6 | `src/cereka_engine_impl.hpp` | Remove `SaveCheckpoint`/`LoadCheckpoint` declarations |
| 7 | `src/cereka_save.cpp` | Revert `readSaveFile` helper + extra functions, keep original `SaveGame`/`LoadGame` |
| 8 | `tests/compile/inputs/checkpoint.crka` | Snapshot test exercising both ops |
| 9 | `CLAUDE.md` | Document `checkpoint store` / `checkpoint load` in syntax block |

## Wave 1 — Core implementation (files 1-5)
Implement the checkpoint system end-to-end: data structure, compiler, bridge, dispatch.

## Wave 2 — Cleanup + tests + docs (files 6-9)
Remove the unused stubs, add snapshot test, run harness, update CLAUDE.md.

## Verification
- `lua tests/compile/harness.lua` — all pass including new checkpoint test
- `ninja -C build` — clean build
