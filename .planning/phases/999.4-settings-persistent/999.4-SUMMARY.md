# Phase 999.4: Persistent Settings — Summary

**One-liner:** Persistent user settings (settings.json) with glaze JSON serialization, wired to DialogueSystem text speed and AudioManager volume.

## Completed
- Created `SettingsManager` class with `SerializableSettings` struct
- Settings file: `settings.json` in project root with fields for textSpeed, bgmVolume, sfxVolume, autoAdvance, fullscreen, skipUnseen
- `Apply()` method wires text speed → DialogueSystem, volume → AudioManager
- Loaded/Applied during `Impl::InitGame()`
- Settings updated live from settings menu (see 999.5)

## Files
- **New:** `src/cereka_settings_manager.hpp`, `src/cereka_settings_manager.cpp`
- **Modified:** `src/cereka_dialogue_system.hpp/.cpp` (configurable speed), `src/cereka_audio_manager.hpp/.cpp` (volume control)

## Commit
`4c97f19`
