---
phase: 02-distribution
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified: [src/save.cpp, src/save_data.hpp, src/engine_impl.hpp]
autonomous: true
requirements: [SAVE-01]

must_haves:
  truths:
    - "Save files are stored in JSON format using the Glaze library"
    - "Save data includes a 'version' field for future migrations"
    - "Engine can save and reload the entire game state including variables, callstack, and scene"
  artifacts:
    - path: "src/save_data.hpp"
      provides: "Glaze-mapped SaveData struct"
    - path: "src/save.cpp"
      provides: "Modernized JSON save/load implementation"
---

<objective>
Modernize the save system using Glaze for JSON serialization and include versioning.

Purpose: Replace the fragile key=value format with a structured, extensible schema.
Output: New SaveData struct and Glaze-based save/load logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-distribution/02-CONTEXT.md
@src/save.cpp
@vendor/glaze/include/glaze/glaze.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define SaveData Schema</name>
  <files>src/save_data.hpp</files>
  <action>
    Create a 'SaveData' struct in src/save_data.hpp:
    - Include fields for: version (int), timestamp (string), pc (size_t), callstack (vector), variables (map), bg (string), characters (map), bgm (string), currentState (int), dialogue state (speaker, name, text, displayedChars), and VM skip state.
    - Use 'struct glaze' meta or GLZ_META to map fields for Glaze serialization.
    - Ensure 'version' is set to a constant (e.g., 1).
    Per D-01.
  </action>
  <verify>
    <automated>grep "glz::meta" src/save_data.hpp || grep "GLZ_META" src/save_data.hpp</automated>
  </verify>
  <done>SaveData struct defined and mapped for JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Glaze Save/Load</name>
  <files>src/save.cpp, src/engine_impl.hpp</files>
  <action>
    - Rewrite SaveGame(slot): Populate a SaveData struct from CerekaImpl state and use 'glz::write_file_json' to save it to "saves/slotN.json".
    - Rewrite LoadGame(slot): Use 'glz::read_file_json' into a SaveData struct and restore CerekaImpl state from it.
    - Update GetSlotTimestamp(slot) to parse the JSON file and extract the timestamp.
    - Remove old .sav logic.
    - Note: This is a "clean break" (D-01), so no migration from .sav is required.
  </action>
  <verify>
    <automated>grep "glz::write_file_json" src/save.cpp</automated>
  </verify>
  <done>Save system migrated to Glaze JSON with versioning support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Disk -> Save System | Save files read from disk are untrusted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | Save System | mitigate | Use Glaze's error handling to prevent crashes on malformed JSON; validate version field |
</threat_model>

<verification>
Save a game to a slot.
Verify that a .json file is created in the 'saves' directory and its content is valid JSON.
Load the game and verify all state (scene, dialogue, variables) is correctly restored.
</verification>

<success_criteria>
Games save and load correctly using the new JSON format.
Save files contain a 'version' field.
The UI correctly displays timestamps from the new JSON format.
</success_criteria>

<output>
After completion, create .planning/phases/02-distribution/02-04-SUMMARY.md
</output>
