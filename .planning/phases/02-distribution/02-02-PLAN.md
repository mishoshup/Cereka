---
phase: 02-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/script_vm.cpp, src/text_renderer.cpp, src/text_renderer.hpp, src/Cereka.cpp, src/draw.cpp]
autonomous: true
requirements: [VM-01, UI-01]

must_haves:
  truths:
    - "Nested if/else blocks in scripts execute with correct branch skipping"
    - "Long dialogue lines wrap automatically within the textbox"
    - "Word wrap width and line spacing are configurable via ui script"
  artifacts:
    - path: "src/script_vm.cpp"
      provides: "Bug-free if/else skipping"
    - path: "src/text_renderer.cpp"
      provides: "Wrapped text rendering using TTF_RenderText_Blended_Wrapped"
  key_links:
    - from: "src/Cereka.cpp"
      to: "src/text_renderer.cpp"
      via: "RenderText call"
---

<objective>
Fix the high-severity nested if/else VM bug and implement themeable word-wrap layout.

Purpose: Ensure basic script correctness and text readability.
Output: Fixed VM logic and modernized text renderer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-distribution/02-CONTEXT.md
@src/script_vm.cpp
@src/Cereka.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix nested if/else VM bug</name>
  <files>src/script_vm.cpp, tests/vm_test.cpp</files>
  <behavior>
    - Nested IF inside false branch should remain skipped until outer ENDIF/ELSE.
    - ELSE block inside skipped IF should NOT restore execution if outer skipDepth > 1.
    - Test case: if(false) { if(true) { say "nested"; } else { say "nested else"; } } say "after";
    - Result: Only "after" should be executed.
  </behavior>
  <action>
    Modify the loop in src/script_vm.cpp:
    - In skipMode, handle Op::ELSE by checking if skipDepth == 1 before setting skipMode = false.
    - Ensure ENDIF properly decrements skipDepth and only exits skipMode when it hits 0.
    - Clean up redundant/unreachable ELSE handling in the main switch.
  </action>
  <verify>
    <automated>./build/tests/vm_test</automated>
  </verify>
  <done>VM correctly handles deep nesting of conditional branches.</done>
</task>

<task type="auto">
  <name>Task 2: Implement themeable word-wrap</name>
  <files>src/text_renderer.cpp, src/text_renderer.hpp, src/Cereka.cpp, src/draw.cpp</files>
  <action>
    - In text_renderer.cpp: Add 'RenderTextWrapped' using TTF_RenderText_Blended_Wrapped.
    - In CerekaImpl (Cereka.cpp): Update 'RenderText' to use the wrapped version.
    - Wire 'wrap_width' and 'line_spacing' from 'UiConfig' (ui_config.hpp) into the rendering call.
    - Per D-02: Default wrap_width to 90% of textbox width.
  </action>
  <verify>
    <automated>grep "TTF_RenderText_Blended_Wrapped" src/text_renderer.cpp</automated>
  </verify>
  <done>Text wraps automatically based on configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Script -> VM | VM executes untrusted script instructions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | D | VM | mitigate | Bounds check PC and skipDepth to prevent stack/heap corruption |
</threat_model>

<verification>
Run the engine with a script containing nested IF/ELSE and verify correct output.
Run a script with long dialogue lines and verify they wrap within the textbox.
</verification>

<success_criteria>
VM bug test cases pass.
Visual verification of wrapped text in the engine.
</success_criteria>

<output>
After completion, create .planning/phases/02-distribution/02-02-SUMMARY.md
</output>
