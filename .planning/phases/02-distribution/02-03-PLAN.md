---
phase: 02-distribution
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified: [src/engine_impl.hpp, src/Cereka.cpp, src/script_vm.cpp, src/draw.cpp, src/state/cereka_states.cpp, src/state/cereka_states.hpp]
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "CerekaImpl delegates main loop control to CerekaStateMachine"
    - "Game logic (dialogue, menus, fades) is encapsulated in concrete state classes"
    - "State transitions are traceable/loggable"
  artifacts:
    - path: "src/state/cereka_states.cpp"
      provides: "Implemented concrete states"
    - path: "src/engine_impl.hpp"
      provides: "Updated CerekaImpl with StateMachine"
  key_links:
    - from: "src/Cereka.cpp"
      to: "src/state/cereka_state.hpp"
      via: "StateMachine delegation"
---

<objective>
"Big Bang" migration to CerekaStateMachine. Decouple game logic from the CerekaImpl God Class into manageable state objects.

Purpose: Improve maintainability, observability, and enable future features like Rollback.
Output: Wired and functional State Machine system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-distribution/02-CONTEXT.md
@src/engine_impl.hpp
@src/state/cereka_state.hpp
@src/state/cereka_states.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CerekaImpl to StateMachine</name>
  <files>src/engine_impl.hpp, src/Cereka.cpp</files>
  <action>
    - Make CerekaImpl inherit from IVNStateContext.
    - Add 'CerekaStateMachine m_stateMachine' to CerekaImpl.
    - Implement IVNStateContext methods (changeState, pushOverlay, popOverlay, etc.) in CerekaImpl.
    - Initialize m_stateMachine in InitGame, registering all concrete states.
    - Delegate HandleEvent, Update, and Draw in CerekaImpl to m_stateMachine.
  </action>
  <verify>
    <automated>grep "m_stateMachine.update" src/script_vm.cpp</automated>
  </verify>
  <done>CerekaImpl correctly delegates lifecycle methods to the state machine.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Concrete States</name>
  <files>src/state/cereka_states.cpp, src/Cereka.cpp, src/script_vm.cpp, src/draw.cpp</files>
  <action>
    Migrate logic from CerekaImpl methods into concrete state implementations:
    - DialogueState::update: Calls TickScript() on the engine.
    - MenuState: Handles menu events and drawing.
    - SaveMenuState / LoadMenuState: Handles slot selection events and drawing the overlay.
    - FadeState: Handles background transitions.
    Move transient data (like fade alpha, menu selection indices) from CerekaImpl into these states where appropriate.
    Per D-03.
  </action>
  <verify>
    <automated>grep -v "void DialogueState::update" src/state/cereka_states.cpp | grep "TickScript"</automated>
  </verify>
  <done>Game logic is successfully relocated to specialized state objects.</done>
</task>

<task type="auto">
  <name>Task 3: Add state transition observability</name>
  <files>src/state/cereka_state.hpp</files>
  <action>
    Add logging to CerekaStateMachine::changeState and push/popOverlay to print transitions to stdout for debugging (e.g., "[STATE] Dialogue -> SaveMenu").
  </action>
  <verify>
    <automated>grep "std::cout" src/state/cereka_state.hpp</automated>
  </verify>
  <done>State transitions are observable in the console.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| State -> Context | States call methods on CerekaImpl via interface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | I | State Machine | mitigate | Ensure popOverlay cannot underflow the stack |
</threat_model>

<verification>
Play through a script and verify dialogue, menus, and saving still work correctly.
Observe state transition logs in the console.
</verification>

<success_criteria>
Engine functions identically to before but using the state machine.
Console logs show correct state transitions.
No regression in input handling or rendering.
</success_criteria>

<output>
After completion, create .planning/phases/02-distribution/02-03-SUMMARY.md
</output>
