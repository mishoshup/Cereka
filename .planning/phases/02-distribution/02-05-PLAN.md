---
phase: 02-distribution
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified: [src/*, tests/*, scripts/*, include/*, .github/*, CMakeLists.txt]
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "All source/header files use cereka_ prefix (SDL-style naming convention)"
    - "IVNState/VNState types renamed to ICerekaState/CerekaState throughout codebase"
    - "bare config namespace moved to cereka::config"
    - "cereka::scenario namespace renamed to cereka::compiler"
    - "script_vm.cpp renamed to cereka_script.cpp; TickScript → CerekaScriptTick"
    - "compiler.lua renamed to cereka_compiler.lua"
    - "All competitor references (Ren'Py, Unity, Unreal) removed from docs and comments"
    - "All 'Visual Novel' references replaced with 'Cereka game' or 'Cereka' in docs"
    - "Build succeeds and all tests pass after rename"
  artifacts:
    - path: "src/cereka_script.cpp"
      provides: "Renamed script execution module"
    - path: "src/cereka_instruction.hpp"
      provides: "Renamed compiler instruction header"
    - path: "src/cereka_instruction.cpp"
      provides: "Renamed compiler instruction implementation"
    - path: "scripts/cereka_compiler.lua"
      provides: "Renamed Lua compiler script"
---

<objective>
Complete the Phase 2 Branding & Naming Conventions: rename all source files, types, namespaces, and docs to use the cereka_ prefix / Cereka brand per CONTEXT.md Section 5. Full path rename — no forwarding shims.

Purpose: Consistent SDL-style naming across the entire codebase. Eliminate VN/VM legacy naming. Remove competitor references from all docs.
Output: Rebranded codebase that builds and passes all tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-distribution/02-CONTEXT.md
@.planning/phases/02-distribution/02-DISCUSSION-LOG.md
@src/CMakeLists.txt
@tests/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename source files (SDL-style cereka_ prefix)</name>
  <files>src/*, src/compiler/*, tests/*, scripts/*</files>
  <action>
    Rename all source/header files per CONTEXT.md Section 5 table:

    | Current | New |
    |---------|-----|
    | src/audio_manager.cpp/hpp | src/cereka_audio_manager.cpp/hpp |
    | src/dialogue_system.cpp/hpp | src/cereka_dialogue_system.cpp/hpp |
    | src/draw.cpp | src/cereka_draw.cpp |
    | src/engine_impl.hpp | src/cereka_engine_impl.hpp |
    | src/menu_system.cpp/hpp | src/cereka_menu_system.cpp/hpp |
    | src/save_data.hpp | src/cereka_save_data.hpp |
    | src/save.cpp | src/cereka_save.cpp |
    | src/scene_manager.cpp/hpp | src/cereka_scene_manager.cpp/hpp |
    | src/script_vm.cpp | src/cereka_script.cpp |
    | src/text_renderer.cpp/hpp | src/cereka_text_renderer.cpp/hpp |
    | src/ui_config.cpp/hpp | src/cereka_ui_config.cpp/hpp |
    | src/video.cpp/hpp | src/cereka_video.cpp/hpp |
    | src/compiler/vn_instruction.cpp/hpp | src/compiler/cereka_instruction.cpp/hpp |
    | tests/vm_test.cpp | tests/cereka_script_test.cpp |
    | scripts/compiler.lua | scripts/cereka_compiler.lua |

    Use git mv for each rename to preserve history.
    Files that stay as-is per CONTEXT.md: include/Cereka/Cereka.hpp, include/Cereka/exceptions.hpp, src/Cereka.cpp (already branded), src/state/* (already cereka_ prefix), scripts/verify-*.sh, scripts/make-appimage.sh, scripts/build_all.sh.
  </action>
  <verify>
    <automated>ls src/cereka_*.cpp src/cereka_*.hpp src/compiler/cereka_*.hpp src/compiler/cereka_*.cpp tests/cereka_*.cpp 2>/dev/null | wc -l</automated>
  </verify>
  <done>All source files renamed with cereka_ prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Update CMakeLists.txt and include paths</name>
  <files>src/CMakeLists.txt, tests/CMakeLists.txt</files>
  <action>
    Update both CMakeLists.txt files to reference the new filenames:
    - src/CMakeLists.txt: Change vn_instruction to cereka_instruction, script_vm to cereka_script, etc.
    - tests/CMakeLists.txt: Change vm_test to cereka_script_test
  </action>
  <verify>
    <automated>grep -c "vn_\|script_vm\|audio_manager\|dialogue_system\b" src/CMakeLists.txt tests/CMakeLists.txt; should be 0</automated>
  </verify>
  <done>CMake build files updated with new filenames.</done>
</task>

<task type="auto">
  <name>Task 3: Update all #include directives across codebase</name>
  <files>src/*, include/*, tests/*</files>
  <action>
    Update every #include directive that references the renamed files. For example:
    - #include "vn_instruction.hpp" → #include "cereka_instruction.hpp"
    - #include "script_vm.hpp" → #include "cereka_script.hpp"
    - #include "save_data.hpp" → #include "cereka_save_data.hpp"
    - #include "engine_impl.hpp" → #include "cereka_engine_impl.hpp"
    - etc. for all renamed files

    Check files in src/, tests/, include/ — both quoted and bracketed includes.
  </action>
  <verify>
    <automated>grep -rn '#include.*vn_\|#include.*script_vm\|#include.*audio_manager\|#include.*dialogue_system\|#include.*draw\.\|#include.*engine_impl\|#include.*menu_system\|#include.*save_data\|#include.*save\.\|#include.*scene_manager\|#include.*text_renderer\|#include.*ui_config\|#include.*video\.' src/ tests/ include/ 2>/dev/null; echo "Count=$(grep -c .)"</automated>
  </verify>
  <done>All include directives updated.</done>
</task>

<task type="auto">
  <name>Task 4: Rename types/functions (VN → Cereka, VM → Cereka Script)</name>
  <files>src/*, include/*, tests/*</files>
  <action>
    Rename types and functions per CONTEXT.md Section 5 tables:

    State Machine & Compiler:
    - `IVNState` → `ICerekaState`
    - `VNState<T>` → `CerekaState<T>`
    - `IVNStateContext` → `ICerekaStateContext`
    - `CompileVNScript()` → `CompileCerekaScript()`
    - `cereka::scenario` → `cereka::compiler`

    VM/Runtime:
    - `TickScript()` → `CerekaScriptTick()`
    - `LoadScript()` → `LoadCerekaScript()`
    - `LoadCompiledScript()` → `LoadCompiledCerekaScript()`

    Search across src/, include/, tests/ for all occurrences and replace.
    Do NOT rename CerekaStateMachine, CerekaEngine, CerekaEvent, CerekaState (public API, already branded).
    Do NOT rename InMenu(), CurrentText(), ButtonCount(), ProgramCounter() (public API, keep as-is).
  </action>
  <verify>
    <automated>grep -rn '\bIVNState\b\|\bVNState\b\|CompileVNScript\|TickScript\|LoadCompiledScript\b\|LoadScript\b\|cereka::scenario' src/ include/ tests/ 2>/dev/null; echo "Count=$(grep -c .)"</automated>
  </verify>
  <done>All types and functions renamed to Cereka brand.</done>
</task>

<task type="auto">
  <name>Task 5: Rename namespaces</name>
  <files>src/*, tests/*</files>
  <action>
    Per CONTEXT.md Section 5:
    - `config` → `cereka::config` (includes `config::parsers`, `config::serializers`, `config::handlers`)
    - `cereka::scenario` → `cereka::compiler`

    For the bare `config` namespace: wrap in `namespace cereka { ... }` or explicitly rename.
    All engine namespaces should be under `cereka::`.

    Check usages in src/ and tests/ (not include/ — public API uses Cereka prefix already).
  </action>
  <verify>
    <automated>grep -rn '^namespace config\b\|^namespace parsers\b\|^namespace serializers\b\|^namespace handlers\b' src/ tests/ 2>/dev/null; echo "Count=$(grep -c .)"</automated>
  </verify>
  <done>All namespaces properly nested under cereka::.</done>
</task>

<task type="auto">
  <name>Task 6: Rebrand documentation and code comments</name>
  <files>CLAUDE.md, README.md, include/Cereka/Cereka.hpp, include/Cereka/exceptions.hpp, src/*, tests/*</files>
  <action>
    Per CONTEXT.md Section 5:
    - Remove all competitor mentions (Ren'Py, Unity, Unreal) from docs and code comments
    - Replace "Visual Novel" with "Cereka game" or "Cereka" in all docs and comments
    - Update CLAUDE.md to remove competitor comparisons, reposition Cereka as its own category
    - Update README.md similarly
    - Do NOT modify vendor/ directory files

    Search for: "Ren'Py", "RenPy", "renpy", "Unity", "Unreal", "visual novel", "Visual Novel"
    Replace instances in project docs and code comments.
  </action>
  <verify>
    <automated>grep -rni "Ren'Py\|RenPy\|renpy\|\bUnity\b\|Unreal\|\bvisual novel\b" CLAUDE.md README.md include/ src/ tests/ 2>/dev/null || echo "No competitor references found"</automated>
  </verify>
  <done>All docs and comments rebranded, competitor references removed.</done>
</task>

<task type="auto">
  <name>Task 7: Update embedded Lua compiler reference</name>
  <files>src/compiler/embed_lua.cmake, src/compiler/cereka_instruction.cpp, scripts/cereka_compiler.lua</files>
  <action>
    Update the cmake embed script (embed_lua.cmake) and any C++ references to the compiler.lua file to point to cereka_compiler.lua.
    Verify the embedded script is loaded as the new filename.
  </action>
  <verify>
    <automated>grep "cereka_compiler.lua" src/compiler/embed_lua.cmake && grep -r "compiler\.lua" src/ --include="*.cpp" --include="*.hpp" --include="*.cmake" 2>/dev/null | grep -v "cereka_compiler"</automated>
  </verify>
  <done>Embedded Lua compiler reference updated.</done>
</task>

<task type="auto">
  <name>Task 8: Update CI and GitHub files referencing old names</name>
  <files>.github/workflows/*</files>
  <action>
    Check .github/workflows/ for any references to renamed files (vm_test, vn_instruction, script_vm, compiler.lua, etc.) and update them.
    Also check if .gitignore references any renamed files.
  </action>
  <verify>
    <automated>grep -rn "vn_\|script_vm\|vm_test\|compiler\.lua\b" .github/ 2>/dev/null; echo "Count=$(grep -c .)"</automated>
  </verify>
  <done>CI files updated with new paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A — renaming/cosmetic changes only, no runtime behavior modification.

## Risks
| Risk ID | Category | Component | Disposition | Mitigation |
|---------|----------|-----------|-------------|------------|
| R-02-05-01 | Missed reference | Any file | mitigate | After rename, do a build + test pass to catch any missed #include or symbol references |
| R-02-05-02 | Collateral rename | Public API | avoid | Check CONTEXT.md Section 5 "Public API Surface" table — CerekaEngine, CerekaEvent, CerekaState, InMenu(), CurrentText(), ButtonCount(), ProgramCounter() stay as-is |
| R-02-05-03 | Docs stale | CLAUDE.md, README | mitigate | Update docs in same pass so they don't reference old names |
</threat_model>

<verification>
1. Build: `ninja -C build cereka_test -j12` succeeds (all files found, no missing includes)
2. Unit tests: `./build/tests/cereka_test` passes all tests
3. No stale references: `grep -rn "vn_\|script_vm\|IVNState\|VNState\|CompileVNScript\|TickScript\|LoadCompiledScript\|compiler\.lua" src/ tests/ include/` returns empty
</verification>

<success_criteria>
- All source files have cereka_ prefix (checked by listing src/cereka_*)
- All types use Cereka brand prefix
- No competitor references in docs or code comments
- Build succeeds with zero errors
- All tests pass
</success_criteria>

<output>
After completion, create .planning/phases/02-distribution/02-05-SUMMARY.md
</output>
