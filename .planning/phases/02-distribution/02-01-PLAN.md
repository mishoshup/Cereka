---
phase: 02-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.github/workflows/ci.yml, scripts/verify-linux.sh, scripts/verify-windows.sh]
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "CI runs on every push to GitHub"
    - "Linux build and tests pass in CI"
    - "Windows cross-build and Wine-tests pass in CI"
    - "macOS build and tests pass in CI"
    - "Local verification scripts provide clean-room environment"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions pipeline"
    - path: "scripts/verify-linux.sh"
      provides: "Docker-based verification"
    - path: "scripts/verify-windows.sh"
      provides: "Wine-based verification"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/verify-linux.sh"
      via: "Workflow step"
---

<objective>
Establish a robust CI/CD pipeline and local verification scripts to ensure engine correctness across all target platforms (Linux, Windows, macOS).

Purpose: Enforce distribution integrity and prevent regressions during the architecture refactor.
Output: GitHub Actions workflow and local shell scripts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-distribution/02-CONTEXT.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions Workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create a multi-platform CI workflow:
    - Linux: Use ubuntu-latest, install dependencies (SDL3, SDL3_image, SDL3_mixer, SDL3_ttf, Lua), build with CMake, run tests.
    - Windows: Use ubuntu-latest, install llvm-mingw, cross-compile for x86_64-w64-mingw32, run tests using 'wine'.
    - macOS: Use macos-latest, install dependencies via brew, build with CMake, run tests.
    Per D-04.
  </action>
  <verify>
    <automated>ls .github/workflows/ci.yml</automated>
  </verify>
  <done>Workflow file created and configured for all 3 platforms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement local verification scripts</name>
  <files>scripts/verify-linux.sh, scripts/verify-windows.sh</files>
  <action>
    - scripts/verify-linux.sh: Create a script that uses 'docker run' with an ubuntu image to build and test the project in a clean environment.
    - scripts/verify-windows.sh: Create a script that uses 'llvm-mingw' and 'wine' to cross-compile and verify the Windows build locally.
    Ensure scripts are executable (chmod +x).
    Per D-04.
  </action>
  <verify>
    <automated>test -x scripts/verify-linux.sh && test -x scripts/verify-windows.sh</automated>
  </verify>
  <done>Scripts created and executable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI -> Runner | Code executed in shared runners |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | scripts | mitigate | Use specific base images/versions to prevent supply chain poisoning |
</threat_model>

<verification>
Check GitHub Actions UI for first run.
Run scripts/verify-linux.sh and scripts/verify-windows.sh locally.
</verification>

<success_criteria>
All CI jobs green on GitHub.
Local verification scripts complete successfully without dependency errors.
</success_criteria>

<output>
After completion, create .planning/phases/02-distribution/02-01-SUMMARY.md
</output>
